<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="locusta.project.mapacti"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" >
    </uses-permission>
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="8" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <uses-library android:name="com.google.android.maps" />

        <activity
            android:name="locusta.project.mapacti.LocustaMapActivityMain"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity android:name="locusta.project.addEvent.AddEventActivity" android:exported="true"></activity> -->
        <activity
            android:name="locusta.project.addEvent.AddEventActivity"
            android:theme="@android:style/Theme.Black" >
        </activity>
        <activity
            android:name="locusta.project.mapSettings.MapSettings"
            android:theme="@android:style/Theme.Black" >
        </activity>

        <service android:name=".TTSService" >
        </service>

        <activity
            android:name="locusta.project.speech.SampleSpeechActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Black" >
        </activity>
    </application>

</manifest>